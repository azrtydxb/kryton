# NAS screenshots — placeholder

Real product screenshots for Synology Package Center and QNAP App Center
listings land here in **Phase 5** (docs phase) per
`docs/design/nas-package-stores.md`.

Required (filenames referenced by the feed renderer):

- `editor.png` — main editor view (used in `snapshot` arrays)
- `banner.png` — wide banner image (QNAP `<bannerImg>`)

Until Phase 5 these URLs are referenced by the feeds but will 404 on disk.
That is intentional — Phase 1 ships templates and the renderer only.

name: pages

on:
  push:
    branches: [master]
    paths:
      - 'site/**'
      - 'docs/**'
      - 'packages/server/openapi.snapshot.json'
      - 'packages/server/src/modules/agents/mcp/tools.ts'
      - 'packages/server/src/modules/plugins/services/types.ts'
      - '.github/workflows/pages.yml'
  workflow_dispatch:

concurrency:
  group: pages
  cancel-in-progress: true

permissions:
  contents: read

env:
  # Forces actions still shipping Node 20 (configure-pages@v5,
  # upload-pages-artifact@v3) to run on Node 24, ahead of GitHub's
  # 2026-06-02 default switch.
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: 'true'

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
      - uses: actions/setup-node@v5
        with:
          node-version: 24
          cache: npm
          cache-dependency-path: site/package-lock.json
      - name: Install dependencies
        working-directory: site
        run: npm ci
      - name: Prebuild (generators)
        working-directory: site
        run: npm run prebuild || echo 'no prebuild yet'
      - name: Build site
        working-directory: site
        run: npm run build
      - uses: actions/configure-pages@v5
      - uses: actions/upload-pages-artifact@v3
        with:
          path: site/dist

  deploy:
    runs-on: ubuntu-latest
    needs: build
    permissions:
      pages: write
      id-token: write
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - id: deployment
        uses: actions/deploy-pages@v4

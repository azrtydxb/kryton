apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "kryton.configMapName" . }}
  labels:
    {{- include "kryton.labels" . | nindent 4 }}
data:
  {{- range $k, $v := .Values.env.config }}
  {{ $k }}: {{ $v | quote }}
  {{- end }}

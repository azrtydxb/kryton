/**
 * Drizzle schema barrel.
 *
 * Phase 1: intentionally empty. Schema definitions are added in Phase 2 of the
 * Postgres + Drizzle migration. The empty barrel exists so the Drizzle client
 * type can reference `typeof schema` and `drizzle-kit generate` has a valid
 * entry point.
 */
export {};
